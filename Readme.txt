程序结构没有整理，但都注释了。
我是基于JuMP直接解的，等式约束就是能量守恒方程那些，然后加进去了不等式约束。
本来想借助IPOPT直接输出最高热效率，但由于IPOPT是内点法，局部最优化方法，找不到全局最高热效率。有时候会算出来明显不是最优值的情况。
于是我外加了黑盒优化器。
Simple_test.jl 是简单回热循环
Recompress_test.jl 是再压缩循环
Recompress_test OPTIM.jl 是再压缩循环的优化

pTh版本：计算难收敛
phT版本：开发中